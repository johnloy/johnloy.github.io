{
  "private": true,
  "name": "johnloy.github.io",
  "description": "Personal resume site built with JSON Resume",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npx resumed render resume.json --theme jsonresume-theme-even --output index.html && open index.html"
  },
  "dependencies": {
    "jsonresume-theme-even": "^0.22.0",
    "puppeteer": "^24.0.0",
    "resumed": "^3.0.0"
  }
}
